# Deterministic finance package

`@pasarai/finance` performs PasarAI currency and margin calculations with
`decimal.js`. Inputs and outputs are decimal strings; currency is rounded to two
decimal places only at output boundaries.
